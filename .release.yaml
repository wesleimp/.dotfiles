name: doties

release:
  github:
    owner: "wesleimp"
    repo: ".dotfiles"

changelog:
  from: remote
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - 'Auto update'
  footer: |
  ---
  Generated with [release-cli](https://github.com/release-cli/release)
